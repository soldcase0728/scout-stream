from processing.rules.regular_hitter import REGULAR_RULES
from processing.rules.slap_hitter import SLAP_RULES
from processing.rules.shared import SHARED_RULES

ALL_RULES = REGULAR_RULES + SLAP_RULES + SHARED_RULES
